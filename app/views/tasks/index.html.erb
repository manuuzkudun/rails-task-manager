<h1>List of tasks</h1>
<ul>
<% @tasks.each do |task| %>
  <li>
    <%= link_to(task) do %>
      <p><%= task.name %></p>
    <% end %>
    <p><%= task.done ? "Done!" : "Not done yet.." %></p>
  </li>
<% end %>
</ul>

<%= link_to "Add a new task", task_new_path %>
